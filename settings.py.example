DATABASES = {
    'default': {
        'ENGINE': 'pg',
        'NAME': '',  # 数据库名字
        'USER': '',  # 用户名
        "PASSWORD": '',  # 自己的密码
        "HOST": '',
        'PORT': 0,
    }
}

TablePrefix = ''

# 特殊表字段映射
# 这里意思是 products.cover_id 关联关系映射 resources表
# 'products': { 数据库表名
#         'cover_id': 'resources' 字段名 => 关联数据表名
#     }
MapRelationTable = {
    'product': {
        'cover_id': 'products'
    }
}
# 特殊字段映射
# 把某个字段名恒定关联到某个表
# create_id 字段 => users 表
MapRelationColumn = {
    'create_id': 'users',
    'modify_id': 'users',
}

ShowEchart = {
    'padding': 300,  # 间距
    'line_count': 5,  # 列数
    'symbol_size': 20,  # 最小图标大小
    'max_symbol_size': 100,  # 最大图标大小
    'show_all': 1  # 是否默认显示全部 0 否 1 是
}
